


// import { Badge } from "@/app/components/ui/badge";
// import { SkillBadgeProps } from "@/lib/interfaces";


// const skillsConfig: { [key: string]: { variant: string; label: string } } = {
//   ADMIN: { variant: "secondary", label: "Admin" },
//   EMPLOYEE: { variant: "outline", label: "Employee" },
// };

// const SkillBadges: React.FC<SkillBadgeProps> = ({ skills }) => {
//   return (
//     <>
//       {skills?.map((skill, index) => {
//         const skillConfig = skillsConfig[skill] || { variant: "outline", label: skill };
//         const { variant, label } = skillConfig;

//         return <Badge key={index} variant={variant as any}>{label}</Badge>;
//       })}
//     </>
//   );
// };

// export default SkillBadges;